"""
config.py — 超参数与全局配置
------------------------------
将所有可调参数集中到一处，避免"魔法数字"散落在各处。
后续只需修改此文件即可切换实验配置。
"""

import os

# ------------------------------------------------------------------ #
#  路径配置
# ------------------------------------------------------------------ #
# 项目根目录（dl_tutorial 2 目录）
BASE_DIR = os.path.abspath(os.path.dirname(__file__))

# 数据文件路径
DATA_PATH = os.path.join(BASE_DIR, 'data', 'train.csv')

# ------------------------------------------------------------------ #
#  数据集配置
# ------------------------------------------------------------------ #
DATA_CONFIG = {
    'test_size': 0.3,       # 测试集比例
    'random_state': 42,     # 随机种子，保证可复现
    'normalize': True,      # 是否对特征做归一化（MinMaxScaler）
}

# ------------------------------------------------------------------ #
#  网络结构配置
# ------------------------------------------------------------------ #
MODEL_CONFIG = {
    'input_size': 784,          # 输入层节点数（28×28=784）
    'hidden_size': 10,          # 隐藏层节点数
    'output_size': 10,          # 输出层节点数（0-9共10类）
    'weight_init_std': 0.01,    # 权重初始化标准差
}

# ------------------------------------------------------------------ #
#  训练超参数配置
# ------------------------------------------------------------------ #
TRAIN_CONFIG = {
    'learning_rate': 0.1,   # 学习率（梯度下降步长）
    'batch_size': 100,      # 每次训练使用的小批量样本数
    'num_epochs': 10,       # 训练轮次

    # 梯度计算方式：
    #   'numerical'   — 数值梯度（当前使用，速度较慢，无需了解反向传播）
    #   'backprop'    — 解析梯度（反向传播，后续学习后替换）
    'grad_method': 'numerical',

    # 优化器类型（预留，暂时只支持 'sgd'）
    #   'sgd'     — 随机梯度下降（当前使用）
    #   'momentum'— 动量法（后续实现）
    #   'adam'    — Adam 优化器（后续实现）
    'optimizer': 'sgd',
}

# ------------------------------------------------------------------ #
#  日志 / 输出配置
# ------------------------------------------------------------------ #
LOG_CONFIG = {
    'print_loss_every': 1,      # 每次迭代都打印 loss（对齐老师代码，数值梯度时可看到训练进度）
    'save_plot': True,          # 是否保存训练曲线图
    'plot_output_dir': os.path.join(os.path.dirname(__file__), 'outputs'),
}
